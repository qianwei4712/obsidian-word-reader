# Office Reader 迭代路线

更新日期：2026-08-02

当前版本：`3.3.0`

当前产品边界：在 Obsidian Desktop 内本地、离线、只读地阅读 `.docx`、
`.pptx` 和 `.xlsx`。显示名称为 **Office Reader**，插件 ID `word-reader`、
仓库名和发布包名继续保持兼容。

从 `3.3.0` 开始进入功能冻结期：暂不增加新格式、移动端、编辑、批注、全库索引
或其他大型用户功能，优先补齐性能基线、资源生命周期、故障恢复和桌面兼容性。

`CHANGELOG.md` 记录已经发布的功能；本文档只记录基线核对、尚未闭环的工作和
未来版本承诺。实验记录和未定方案保留在被忽略的
`ITERATION_PLAN.local.md`。

## 已发布版本核对

| 版本 | 状态 | 实际交付 |
| --- | --- | --- |
| `1.6.0` | 已发布 | DOCX 大纲增强、阅读状态 LRU，以及生命周期、状态、诊断、缩放和资源释放等共享基础模块 |
| `2.0.0` - `2.0.2` | 已发布 | PPTX 本地只读 MVP、ZIP 预检和资源治理；显示名称改为 Office Reader，插件 ID 保持 `word-reader` |
| `2.1.0` | 已发布 | PPTX 缩略图、标题导航、全文搜索、备注、复制、快捷键和摘要笔记 |
| `2.2.0` | 已发布 | 缩略图窗口化、渲染取消、资源治理、隐私安全诊断和结构化视觉回归 |
| `2.3.0` - `2.3.2` | 已发布 | DOCX/PPTX 全局性能治理、`500 KiB` bundle 预算、兼容性修复 |
| `2.4.0` | 已发布 | 共享阅读壳层和适配器/会话/能力契约、设置迁移、`100` 条统一阅读状态和通用 UI 约定 |
| `2.5.0` | 已发布 | 统一 OOXML 防护、XLSX 验证内核、合法样本库、`100,000` 行基准和依赖审计 |
| `3.0.0` | 已发布 | XLSX 本地只读 MVP、虚拟网格、冻结/合并/样式/格式、公式缓存、搜索、复制和安全超链接 |
| `3.1.0` | 已发布 | XLSX 跨表导航与搜索、命名区域、公式栏、TSV/Markdown 导出和摘要笔记 |
| `3.2.0` | 已发布 | XLSX 旧式批注、包内图片、缓存图表预览、条件格式子集、流式分片解析和稠密大表基准 |
| `3.3.0` | 当前版本 | DOCX/PPTX/XLSX 统一性能基线、五次独立进程中位数/P95、机器可读 CI 趋势产物、Desktop 校准表和隐私安全性能诊断 |

版本记录有一项历史不一致：`2.0.0` 已存在于 `CHANGELOG.md`、
`versions.json` 和本地发布产物中，但当前 Git 历史没有独立的 `2.0.0` 标签，
PPTX MVP 与 `2.0.1` 修复合并在 `2.0.1` 发布提交中。后续发布不得补造或重写
历史标签；新版本的提交、标签、元数据和发布产物必须一一对应。

## 当前质量缺口

- 现有自动测试覆盖单次生命周期和资源释放，但没有跨格式反复打开、重载、切换
  和关闭的长循环泄漏测试。
- Node.js 基准可以稳定验证解析器和窗口算法，但不能替代 Obsidian/Electron
  运行时中的布局、绘制、长任务和真实 DOM 资源观测。
- 跨格式基准和 Desktop 校准表已经建立，但仍需积累不同 CI 运行器、操作系统和
  真实文档的趋势数据，持续校准 Node.js 门禁与实际 Electron 体验。
- 损坏设置、保存中断、文件被替换、工作区恢复和依赖升级后的故障恢复矩阵仍需
  系统化覆盖。

## 后续版本路线

| 版本 | 主题 | 计划范围 |
| --- | --- | --- |
| `3.4.0` | 生命周期与故障恢复 | 增加跨格式反复打开/重载/切换/关闭压力测试；治理 Blob URL、缓存、任务、定时器、观察器和监听器泄漏；覆盖损坏设置、文件替换、工作区恢复和异常中断 |
| `3.5.0` | 超大文件与主线程响应 | 在基准证明瓶颈后优化 DOCX 分批提交、PPTX 调度和 XLSX 流式解析；治理长任务、内存压力和后台任务抢占；建立 CI 与扩展规模两级大文件样本 |
| `3.6.0` | 桌面兼容性与 LTS | 建立 Obsidian 最低/当前稳定版本及 Windows/macOS/Linux 的兼容矩阵；扩展主题、缩放、DPI、视觉回归、安装升级和长时间运行验证；形成稳定维护线 |

`4.0.0` 暂不排期。只有在上述质量版本完成且确实需要改变存储、索引或兼容性
边界时，才启动新的主版本。

## `3.3.0` 验收

- `npm run performance:check` 同时覆盖 DOCX、PPTX 和 XLSX，不再只有 XLSX
  定量基准与 DOCX/PPTX 结构断言。
- 每种格式至少记录：包加载/解析、首个可读内容、搜索、导航或滚动、峰值堆、
  最大 DOM 节点、缓存上限、取消结果和清理结果。
- 保留当前 XLSX 预算：首屏 `2,000 ms`、滚动帧 P95 `20 ms`、峰值堆
  `192 MiB`、DOM `3,000`、稠密解析 `3,000 ms`、工作表数据缓冲
  `256 KiB`；只有真实样本证明不合理时才能调整。
- DOCX/PPTX 先在独立进程中测量不少于 `5` 次，记录中位数与 P95，再将经验证
  的阈值写入 `benchmarks/*.json`；不得根据单次本机结果制定紧预算。
- CI 保存机器可读基准结果，性能回归必须能定位到格式、阶段和指标；同一指标
  超预算时发布失败。
- 增加 Obsidian Desktop 手动基线，记录插件激活、首屏、长任务、实际 DOM 和
  文件关闭后的资源数量，用来校准 Node.js 微基准。
- 性能诊断不得包含正文、备注、单元格值、内部 XML 或 vault 绝对路径。
- 继续保持 `dist/main.js <= 500 KiB` 和 release zip `<= 8 MiB`。

## `3.4.0` 验收

- 自动压力测试按 DOCX、PPTX、XLSX 及混合顺序完成至少 `100` 次打开、重载、
  快速导航、文件切换和关闭循环。
- 每轮结束后，Blob URL、格式缓存、后台任务、定时器、观察器和事件监听器回到
  明确基线；强制 GC 后不得出现持续单调增长的保留堆。
- 过期解析、搜索、缩略图、工作表和绘图任务不得提交 UI；取消或关闭过程中
  不产生未处理 Promise rejection。
- 设置数据损坏、旧 schema、部分写入和未知字段均能回退到安全默认值；不会
  覆盖仍可迁移的数据。
- 文件 `mtime` 或内容在读取期间变化时，当前会话安全失效并提供重载，不混用
  旧缓存、阅读状态或搜索结果。
- Obsidian 工作区恢复时，文件缺失、格式变更、视图重复和上次异常退出均不会
  阻止插件启动。
- 损坏、加密、超限和不支持文件持续显示可恢复错误，不造成崩溃或资源残留。

## `3.5.0` 验收

- 快速 CI 样本继续覆盖 `100` 页 DOCX、`1,000` 页 PPTX、`100,000` 行稀疏
  XLSX 和 `20,000` 行/`80,000` 单元格稠密 XLSX。
- 扩展基准覆盖 `500` 页 DOCX、`2,000` 页 PPTX、接近 Excel 行上限的稀疏表
  和更高密度工作簿；扩展基准可定时或发布前运行，不拖慢每次提交。
- 只有基准确认属于热点的解析或索引工作才迁移到 Worker/分时任务，避免无依据
  的并发复杂度。
- 可取消后台工作持续采用有界批次；用户滚动、切页、切表和关闭文件时，高优先级
  交互可以抢占后台索引、缩略图和预加载。
- Obsidian 实测中的单个非必要主线程长任务不超过 `50 ms`；无法切分的浏览器
  布局或第三方渲染阶段必须单独记录并提供降级策略。
- 达到内存、图片、DOM 或解压预算时停止预加载并降低并发，必要时终止渲染并
  保留重试或外部打开入口，不允许进程无上限增长。
- 优化后必须同时验证输出一致性、取消、资源释放和视觉基线，不能用降低阅读
  正确性换取基准数字。

## `3.6.0` 验收

- 支持矩阵至少覆盖 `manifest.json.minAppVersion` 与发布时当前 Obsidian Desktop
  稳定版，以及 Windows、macOS、Linux 的安装、升级、打开、切换和卸载流程。
- 覆盖默认浅色/深色主题、代表性社区主题、100%/150%/200% 系统缩放、窗口
  缩放和多显示器 DPI 切换。
- DOCX/PPTX/XLSX 的合法样本库都有结构和视觉回归；视觉差异必须经过确认后才
  更新基线。
- 发布候选完成长时间阅读、反复切换、睡眠/唤醒和文件变化 soak 测试，没有
  P0/P1 缺陷、未处理异常或持续资源增长。
- 依赖升级必须附带许可证、安全、bundle、性能和视觉差异；无明确收益时不追随
  非必要大版本升级。
- 建立 LTS 维护规则：该阶段之后优先发布安全修复、兼容修复和性能回归修复，
  新功能重新进入路线前必须有独立收益和风险评估。

## 延后功能

以下方向保留，但不进入 `3.3.0` - `3.6.0`：

- DOCX 章节、PPTX 幻灯片和 XLSX 区域的稳定深链接与来源引用。
- 保存到 Markdown 侧车文件的高亮、书签和只读批注。
- vault 内 Office 文档的本地全文索引和跨文件搜索。
- 批量摘要、结构化导出、元数据面板和 Canvas/其他插件集成。
- DOCX 修订/脚注、PPTX 图表/SmartArt、XLSX 数据透视表等进一步格式补强。

明确不做：

- Obsidian Mobile 支持。
- Office 文件编辑和源文件写回。
- 宏、脚本、公式重算、外部数据连接或自动远程资源。
- 云端转换、联网解析或 PDF 阅读器。

## 质量与发布门槛

每个版本继续通过：

- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm run performance:check`
- `npm run dependencies:audit`
- `npm run security:scan`
- `npm run release:check`

发布前还必须核对 `package.json`、`package-lock.json`、`manifest.json`、
`versions.json`、`CHANGELOG.md`、Git 标签和 release zip 版本一致；发布包只
包含 `main.js`、`manifest.json` 和 `styles.css`。
