# Office Reader 迭代路线

更新日期：2026-07-27

当前版本：`2.4.0`

当前产品边界：Obsidian Desktop 内本地、离线、只读地阅读 `.docx` 和
`.pptx`。显示名称为 **Office Reader**，插件 ID `word-reader`、仓库名和发布
包名继续保持兼容。

`CHANGELOG.md` 记录已经发布的功能；本文档只记录基线核对、尚未闭环的工作和
未来版本承诺。依赖候选、实验记录和未定方案保留在被忽略的
`ITERATION_PLAN.local.md`。

## 已发布版本核对

| 版本 | 状态 | 实际交付 |
| --- | --- | --- |
| `1.6.0` | 已发布 | DOCX 当前章节高亮、可折叠大纲、按文件恢复阅读状态；抽取生命周期、状态、诊断、缩放、外部打开和资源释放等共享基础模块；阅读状态使用最近 `50` 个文件的有界 LRU |
| `2.0.0` - `2.0.2` | 已发布 | PPTX 本地只读 MVP、ZIP 预检、过期渲染取消和资源释放；显示名称改为 Office Reader，插件 ID 保持 `word-reader` |
| `2.1.0` | 已发布 | PPTX 缩略图、标题导航、全文搜索、备注、文本复制、快捷键和摘要笔记 |
| `2.2.0` | 已发布 | 缩略图窗口化挂载、渲染取消与资源治理、隐私安全诊断、兼容性样本和结构化视觉回归 |
| `2.3.0` | 已发布 | PPTX 首屏优先、增量索引、优先级队列、分时渲染、导航虚拟化和有界缓存；DOCX 搜索与状态写入优化；移除 Mammoth；建立大文件回归和 `500 KiB` 生产 bundle 预算 |
| `2.3.1` | 已发布 | 修复 Obsidian `1.12.7` 设置兼容路径中的官方 lint 检查问题 |
| `2.3.2` | 已发布 | 修复 DOCX 大纲对本地化、自定义及直接设置大纲级别的标题识别 |
| `2.4.0` | 当前版本 | 共享阅读壳层和适配器/会话/能力契约；DOCX/PPTX 视图职责拆分；版本化分区设置迁移；`100` 条带 `mtime` 的统一阅读状态；通用 CSS、诊断和摘要笔记约定 |

版本记录有一项历史不一致：`2.0.0` 已存在于 `CHANGELOG.md`、
`versions.json` 和本地发布产物中，但当前 Git 历史没有独立的 `2.0.0` 标签，
PPTX MVP 与 `2.0.1` 修复合并在 `2.0.1` 发布提交中。后续发布不得补造或重写
历史标签；以现有 Changelog 说明该情况，并确保新版本的提交、标签、元数据和
发布产物一一对应。

## 尚未闭环

`2.4.0` 已完成加入 XLSX 前的阅读器架构收口。以下工作留给后续版本：

- OOXML ZIP 防护目前是 PPTX 专属实现，默认限制为 `2,000` 个条目、
  单项 `64 MiB`、总计 `256 MiB` 和 `200:1` 压缩比，尚未成为跨格式策略。
- 还没有第三方许可证清单和顶层运行时依赖精确版本策略。
- `manifest.json.isDesktopOnly` 仍为 `true`，运行时仍包含受保护的
  Electron/Node.js 桌面能力。

## 版本路线

| 版本 | 主题 | 计划范围 |
| --- | --- | --- |
| `2.4.0` | 架构收口与一致体验 | 建立共享阅读壳层和统一适配器/会话/能力接口；拆分 DOCX/PPTX 视图职责；增加设置 schema 与迁移器、`100` 条阅读状态 LRU、通用 CSS 类；统一工具栏、状态、错误、诊断和摘要笔记约定 |
| `2.5.0` | XLSX 发布前验证 | 建立统一 OOXML 包防护和合法样本库；完成 XLSX 解析、样式、公式安全、稀疏大表、虚拟网格和内存基准；完成依赖许可证、精确版本和包体积审计；不注册公开 `.xlsx` 视图 |
| `3.0.0` | XLSX 只读 MVP | 支持工作表切换、虚拟网格、冻结窗格、合并单元格、行列尺寸、基础样式、数字/日期格式、公式文本与缓存结果、搜索、缩放、区域复制和外部打开 |
| `3.1.0` | 工作簿导航与知识提取 | 增加名称框、单元格跳转、工作簿级搜索、隐藏工作表提示、命名区域、公式栏、选区转 TSV/Markdown 和摘要笔记 |
| `3.2.0` | XLSX 丰富内容与大文件 | 增加批注、图片、部分图表、条件格式子集和超大工作簿分片解析；根据安全与兼容测试再决定是否只读支持 `.xlsm` |
| `3.3.0` | 移动端准备 | 隔离 Electron/Node.js 能力，增加移动兼容扫描、触控交互、低内存策略和 Android/iOS 真机基线；继续保持桌面端限定 |
| `4.0.0` | DOCX 移动阅读 MVP | 真机验收通过后将 `isDesktopOnly` 改为 `false`；先开放 DOCX 的打开、滚动、缩放、搜索、复制、大纲和摘要笔记 |
| `4.1.0` | PPTX 移动阅读 | 开放逐页阅读、触控缩放、滑动翻页、缩略图和文本搜索，采用更严格的移动端资源预算 |
| `4.2.0` | XLSX 移动阅读 | 开放工作表切换、触控虚拟网格、冻结窗格、搜索和区域复制，补充小屏选区操作 |
| `4.3.0` | 移动性能与无障碍 | 完善前后台恢复、内存压力降级、横竖屏切换、屏幕阅读器、大字体和平台差异诊断 |

不加入 PDF；Obsidian 已提供成熟的核心 PDF 阅读能力。

## `2.4.0` 验收

- 保留 `word-reader-view` 和 `pptx-reader-view` 兼容标识，内部通过共享壳层和格式
  适配器分派能力。
- `OfficeReaderAdapter` 负责格式识别、能力声明、文件打开和文本提取；
  `ReaderSession` 负责渲染、导航、搜索、复制、状态恢复和资源释放；
  `ReaderCapabilities` 驱动工具栏功能可见性。
- DOCX、PPTX 格式逻辑进入独立目录，现有视图类只保留 Obsidian 视图接入和会话
  编排；XLSX 不得进入 `WordView.ts` 或 `PptxView.ts`。
- 设置数据带显式 schema 版本，旧数据可无损迁移；格式设置分别存入 `docx`、
  `pptx` 和预留的 `xlsx`。
- 阅读状态最多保存 `100` 条，只包含路径、`mtime`、格式、位置、缩放和导航
  状态，不保存正文、备注、单元格内容或内部 XML。
- 新增 `office-reader-*` 通用类；现有 `word-reader-*` 和 `pptx-reader-*`
  类在整个 `3.x` 保持兼容。
- 快速切换文件、页面或视图不会提交过期结果，也不会遗留 Blob URL、定时器、
  观察器或事件监听器。

## `2.5.0` 验收

- 统一 OOXML 防护的全局硬上限不宽于：`10,000` 个条目、单项 `128 MiB`、
  总解压 `512 MiB`、压缩比 `200:1`；各格式可以使用更严格的限制，PPTX
  当前更严格的默认值不得无理由放宽。
- 拒绝加密包、ZIP64 超限包、宏、OLE 对象和脚本型媒体；忽略外部关系、数据
  连接和自动远程资源，不执行公式重算。
- 样本库覆盖正常、损坏、加密、恶意压缩和极端规模文件；XLSX 覆盖合并、冻结、
  隐藏表、日期、数字格式、公式缓存、稀疏数据、超链接、图片和 `100,000` 行
  虚拟滚动。
- XLSX 只创建可视区域和有限 overscan DOM；基准记录首屏、滚动帧耗时、峰值
  内存、节点数和取消加载结果。
- 生成第三方许可证清单，顶层运行时依赖使用精确版本，构建产物执行依赖审计。
- `2.x` 保持当前 `500 KiB` 生产 bundle 预算；如 XLSX 方案无法满足 `3.0.0`
  不超过 `8 MiB` 的发布包目标，必须拆分或更换依赖。
- 研究代码不得注册 `.xlsx` 扩展名、公开功能开关或技术预览入口。

## `3.0.0` 验收

- 大型稀疏工作表不得按完整行列范围创建 DOM，只保留可视区域和有限 overscan。
- 公式只显示公式文本和文件已有缓存结果，不执行宏、外部公式代码或重新计算。
- 表格复制默认复制显示值；复制公式必须是单独操作。
- 超链接只在用户点击并确认后打开；外部工作簿引用和数据连接不自动访问网络。
- `.xls` 只显示格式说明与外部打开入口；`.xlsm`、完整图表还原、数据透视表、
  切片器和复杂条件格式不在首版承诺范围内。
- 快速切换工作表、滚动、搜索或关闭文件时可以取消解析和渲染，并释放缓存资源。
- 发布包不超过 `8 MiB`，同时通过性能、安全、许可证和发布内容检查。

## 移动端验收边界

- `3.3.0` 完成前不修改 `isDesktopOnly`；移动入口和格式阅读路径不得导入
  Electron、Node.js 内置模块或 `FileSystemAdapter`。
- `4.0.0` 只有在 Android 和 iOS 真机均通过安装、启动、打开、切换、后台恢复
  和关闭文档测试后，才将 `isDesktopOnly` 改为 `false`。
- `4.0.0` 只承诺 DOCX；PPTX/XLSX 在各自移动版本发布前必须安全降级，不得
  导致插件启动或文件切换失败。
- PPTX 移动端最多保留当前页及相邻页；XLSX 仅保留可视单元格和有限 overscan。
- 核心阅读操作必须支持触控、键盘和屏幕阅读器，不依赖悬停、右键或桌面文件
  对话框。

## 质量与发布门槛

每个版本继续通过：

- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm run performance:check`
- `npm run security:scan`
- `npm run release:check`

发布前还必须核对 `package.json`、`package-lock.json`、`manifest.json`、
`versions.json`、`CHANGELOG.md`、Git 标签和 release zip 版本一致；发布包只
包含 `main.js`、`manifest.json` 和 `styles.css`。
